# training-mongo-fitbot

To run enter ```docker-compose up``` from the command line.

This will start a mongo instance configured with the fitbot_db database and user fitbotUser using the default port 27017.